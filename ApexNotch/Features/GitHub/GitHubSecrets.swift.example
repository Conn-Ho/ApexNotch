// GitHubSecrets.swift — NOT committed to git (see .gitignore)
// 1. Register an OAuth App at https://github.com/settings/applications/new
//    - Authorization callback URL: apexnotch://oauth
//    - Enable Device Flow: ✅
// 2. Copy this file to GitHubSecrets.swift and fill in your values.

enum GitHubSecrets {
    static let clientID     = "YOUR_CLIENT_ID"
    static let clientSecret = "YOUR_CLIENT_SECRET"
}
